# AGENTS.md

这份文件为后续在本仓库工作的 coding agent 提供项目上下文、开发约定和验证清单。

## 项目概览

DIY Pearl 是一个零依赖的浏览器端拼豆 / 烫珠模拟器。它使用 HTML5 Canvas 渲染可编辑的拼豆网格，支持画笔、橡皮、吸管、撤销 / 重做、调整画布尺寸、自定义颜色、导出图片，以及通过“熨烫”模式展示相邻同色豆子融合后的效果。

当前界面语言是中文（`zh-CN`）。除非用户明确要求，新增可见文案也应保持中文。

## 技术栈

- 原生 HTML、CSS、JavaScript。
- 当前没有框架、构建工具、包管理器、转译器或测试框架。
- `index.html` 直接按顺序加载各个脚本文件。
- JavaScript 模块通过 `window.DiyPearl` 暴露全局对象。

## 运行与验证

本项目没有构建步骤。

最简单的运行方式是在浏览器中直接打开 `index.html`。如果需要通过本地服务器测试，可以在仓库根目录运行：

```bash
python3 -m http.server 8000
```

然后打开 `http://localhost:8000/`。

修改后建议手动验证：

- 点击和拖拽画笔绘制。
- 使用橡皮擦除格子。
- 使用吸管选择已有颜色。
- 绘制、擦除、清空、调整尺寸后的撤销与重做。
- 调整画布尺寸时，重叠区域的已有内容是否保留。
- 熨烫模式的开启与关闭。
- 导出图片是否正常。
- 如果改动了画布交互或布局，需要检查移动端 / 触摸交互。

## 仓库结构

- `index.html`：页面入口、DOM 结构、脚本加载顺序。
- `style.css`：全局样式和响应式布局。
- `src/main.js`：应用初始化、DOM 查询、顶层事件绑定。
- `src/grid.js`：网格数据模型，包含清空、读写、调整尺寸、克隆等能力。
- `src/history.js`：基于网格克隆的撤销 / 重做栈。
- `src/palette.js`：默认色板、颜色选择、自定义颜色添加。
- `src/tools.js`：画笔、橡皮、吸管，以及鼠标 / 触摸绘制行为。
- `src/canvas.js`：Canvas 尺寸适配、豆子渲染、导出渲染、熨烫模式切换。
- `src/iron.js`：同色连通区域计算和熨烫效果渲染。
- `README.md`：面向用户的项目介绍和功能说明。

## JavaScript 约定

- 保持当前的全局命名空间写法：

```js
window.DiyPearl = window.DiyPearl || {};
window.DiyPearl.SomeClass = class SomeClass {};
```

- 不要在普通改动中把项目改成 ES Modules，除非任务明确要求整体调整加载模型。
- 如果文件依赖其他全局对象，需要确保 `index.html` 的脚本加载顺序正确。当前顺序是：

```text
grid.js -> history.js -> palette.js -> iron.js -> canvas.js -> tools.js -> main.js
```

- 状态变更应尽量保持集中：
  - 网格数据变更优先通过 `Grid` 方法完成。
  - 会影响用户可见结果的操作，应在第一次实际变更前调用 `history.push(grid)`。
  - 需要刷新画面时调用 `renderer.render()`；尺寸相关变更通常需要调用 `fitToContainer()`。
- 现有 JS 风格是两个空格缩进、使用分号、字符串多用单引号、类方法保持简洁。新增代码应匹配这一风格。
- 优先写清晰的小函数，不要为了轻微复用引入过重抽象。这个项目应继续保持轻量。

## Canvas 与交互注意事项

- `CanvasRenderer.fitToContainer()` 会根据当前网格和容器尺寸重新计算 `cellSize`、canvas 宽度和高度。
- `getCellFromPoint()` 使用 canvas 的 bounding rect 和当前 `cellSize` 将指针坐标转换为网格坐标。
- `Tools` 目前分别处理鼠标事件和触摸事件。如果改成 Pointer Events，需要注意避免触摸设备重复触发。
- 熨烫模式只是 `CanvasRenderer` 上的视觉状态，不是对网格数据的破坏性转换。切换熨烫模式时不要修改 `grid.data`。
- `exportImage()` 会根据当前模式导出图片：普通模式导出圆形豆子，熨烫模式导出融合后的熨烫效果。修改导出语义属于用户可见行为变更，需要谨慎。
- 移动端支持在画布上双指开合缩放；缩放只改变 canvas 绘制尺寸，不应影响底部操作栏尺寸。

## CSS 与体验约定

- 这是一个编辑工具，不是营销页。首屏应始终优先呈现可用画布和编辑控件。
- 除非任务是重设计，否则保持当前侧边栏 + 画布的工作台布局和紧凑控件风格。
- 颜色、圆角、阴影、尺寸等优先复用 `:root` 中的 CSS 自定义属性。
- 修改样式后同时检查桌面端和窄屏移动端布局。
- 避免控件、色块、画布区域因为 hover、active、禁用状态或文本变化发生明显布局跳动。

## 安全与边界

- 当前没有 `.gitignore`、依赖锁文件、构建产物目录或依赖目录。除非任务明确要求，不要引入依赖工具链。
- 不要为核心功能引入运行时依赖的网络资源。
- 改动应尽量小而聚焦。仓库很小，无关重构会非常显眼，应避免。
- 谨慎处理撤销 / 重做：历史记录存的是完整网格克隆。任何修改 `grid.width`、`grid.height` 或 `grid.data` 的逻辑，都应维持这个契约。
